% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hypo_test.R
\name{hypo_test}
\alias{hypo_test}
\title{Hypothesis Tests for ATE Marginal Treatment Means}
\usage{
hypo_test(
  object,
  type = c("all", "global", "pairwise", "custom"),
  C = NULL,
  rhs = NULL,
  alpha = 0.05,
  p_adjust = "none"
)
}
\arguments{
\item{object}{An ATE inference object returned by \code{\link{lbc_net}} or
\code{\link{m_lbcnet}} with an outcome supplied and variance estimation
enabled. The object must contain \code{means} and \code{covariance}; a propensity
score fit alone or a numeric result from \code{\link{est_effect}} is
insufficient. Older binary fits without these fields must be refitted.}

\item{type}{One of \code{"all"} (default), \code{"global"}, \code{"pairwise"}, or \code{"custom"}.}

\item{C}{Numeric, finite, full-row-rank contrast matrix for \code{type = "custom"}.
There must be one column per treatment. Unnamed columns follow the row
order of \code{object$means}. If column names are supplied, they must identify
all treatment labels and are aligned to that order.}

\item{rhs}{Numeric, finite right-hand side of the custom null hypothesis,
with length \code{nrow(C)}. Defaults to a zero vector. \code{C} and \code{rhs} are only
accepted for \code{type = "custom"}.}

\item{alpha}{Significance level used for pairwise confidence intervals,
strictly between zero and one. Default is \code{0.05} (95\% intervals).}

\item{p_adjust}{Pairwise p-value adjustment method accepted by
\code{\link[stats:p.adjust]{stats::p.adjust()}}, including \code{"none"} (default) and \code{"holm"}.
Adjustment applies to the family of all pairwise comparisons and does
not change the global/custom test or the confidence intervals.}
}
\value{
For \code{type = "all"}, a list with \code{global} and \code{pairwise} data frames.
For \code{"global"}, a one-row data frame containing \code{hypothesis}, \code{statistic}
(Wald chi-square), \code{df}, and \code{p_value}. For \code{"pairwise"}, a data frame
containing \code{treatment_1}, \code{treatment_2}, \code{estimate}, \code{se}, \code{statistic}
(signed z), \code{p_value}, \code{ci_lower}, \code{ci_upper}, and \code{p_adjusted}.
For \code{"custom"}, a list containing \code{statistic} (Wald chi-square), \code{df},
\code{p_value}, \code{contrast_estimate = as.vector(C \%*\% mu_hat - rhs)}, and
\code{contrast_covariance = C \%*\% V_mu \%*\% t(C)}.
}
\description{
Performs global, pairwise, or custom linear hypothesis tests
using the estimated marginal treatment means and their full stored joint
influence-function covariance matrix.
}
\details{
This implementation applies to the current ATE inference framework.
Let \eqn{\hat\mu} contain the marginal treatment mean estimates and let
\eqn{V_\mu} be their stored joint covariance matrix. No covariance is
reconstructed from marginal standard errors.

The global null is \eqn{H_0: \mu_1 = \cdots = \mu_L}. It uses
\code{cbind(diag(L - 1), -rep(1, L - 1))}, comparing each of the first
\eqn{L-1} means with the last. The alternative is that at least one mean
differs. The Wald statistic is asymptotically chi-square with \eqn{L-1}
degrees of freedom. For two groups it equals the squared pairwise z
statistic, up to numerical precision.

Custom tests evaluate \eqn{H_0: C\mu = \mathrm{rhs}} using
\deqn{W = (C\hat\mu - \mathrm{rhs})^T
  (C V_\mu C^T)^{-1}(C\hat\mu - \mathrm{rhs}).}
The reference distribution is chi-square with \code{rank(C)} degrees of freedom.
Covariances are symmetrized locally to remove floating point asymmetry;
the stored estimator is not changed. A singular or numerically singular
contrast covariance raises an error; no generalized inverse is used.

Pairwise tests evaluate \eqn{H_0: \mu_t - \mu_s = 0} for each pair in
the stored treatment order, retaining the original labels. Their standard
errors use the full covariance, and their two-sided p-values use the
asymptotic standard normal distribution. Binary fits store means in order
\verb{1, 0}, so the contrast remains treatment minus control, consistent with
the existing binary effect. M-LBCNet retains its fitted treatment order
and the direction of \code{pairwise_ate}.

Confidence intervals use \code{qnorm(1 - alpha / 2)}. Existing fit intervals
use the rounded multiplier \code{1.96}; those stored intervals are preserved,
so their endpoints can differ slightly from these intervals at \code{alpha = 0.05}.

Tests are attached automatically as \code{object$hypothesis_test} when an ATE
fit computes outcome inference. For M-LBCNet, tests are not calculated
when \code{compute_variance = FALSE}. If the contrast covariance is singular,
automatic testing warns and stores \code{hypothesis_test = NULL}, preserving
the completed fit and its estimates. An explicit \code{hypo_test()} call still
raises an error in that case. Calling this function again allows a
different test, confidence level, or multiplicity adjustment.
}
\examples{
\dontrun{
binary_ate_result <- lbc_net(Z = Z, Tr = Tr, Y = Y, estimand = "ATE")
h <- hypo_test(binary_ate_result)
h$global
h$pairwise

multi_ate_result <- m_lbcnet(Z = Z, Tr = treatment, Y = Y)
h <- hypo_test(multi_ate_result)
h$global
h$pairwise
multi_ate_result$hypothesis_test

# One three-treatment mean versus the average of the other two
C <- matrix(c(1, -0.5, -0.5), nrow = 1)
hypo_test(multi_ate_result, type = "custom", C = C)
hypo_test(multi_ate_result, type = "custom", C = C, rhs = 1)
hypo_test(multi_ate_result, type = "pairwise", p_adjust = "holm")
}
}
