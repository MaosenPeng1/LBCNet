% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_m_lbcnet_lsd.R
\name{plot.m_lbcnet_lsd}
\alias{plot.m_lbcnet_lsd}
\title{Plot Multi-Treatment Local Standardized Mean Differences}
\usage{
\method{plot}{m_lbcnet_lsd}(
  x,
  y = NULL,
  type = c("versus_population", "pairwise"),
  cov = "ALL",
  box.loc = seq(0.1, 0.9, by = 0.2),
  ...,
  localizing_treatment = NULL,
  treatment_pair = NULL,
  point.color = "#9467bd",
  point.size = 0.8,
  line.color = "black",
  line.size = 0.5,
  theme.size = 15,
  boxplot.width = 0.02,
  outlier.shape = 4,
  outlier.size = 1
)
}
\arguments{
\item{x}{An object of class \code{m_lbcnet_lsd} returned by
\code{\link{lsd}} for an \code{\link{m_lbcnet}} fit.}

\item{y}{Unused; included for compatibility with the \code{plot()} generic.}

\item{type}{Diagnostic to display. \code{"versus_population"} (the default)
compares each treatment with the common local ATE population.
\code{"pairwise"} compares treatment pairs within each localizing treatment's
generalized propensity score neighborhoods.}

\item{cov}{\code{"ALL"} (the default) averages LSD over covariates at each
center within each panel. Otherwise, supply one covariate name or a
one-based numeric index in the order of the fitted covariate columns.}

\item{box.loc}{Numeric vector of centers at which to show boxplots over
covariates when \code{cov = "ALL"}. Defaults to \code{seq(0.1, 0.9, by = 0.2)}.
Matching uses \code{\link[dplyr:near]{dplyr::near()}} to allow floating point differences;
unmatched locations are ignored. Explicit \code{NULL} disables boxplots.
Ignored when a specific covariate is selected.}

\item{...}{Additional arguments, currently unused.}

\item{localizing_treatment}{Optional treatment label or vector of labels
selecting pairwise facet rows. \code{NULL} shows all localizing treatments.
Only used when \code{type = "pairwise"}.}

\item{treatment_pair}{Optional vector of two distinct treatment labels
selecting a pairwise comparison, in either order. \code{NULL} shows all pairs.
Only used when \code{type = "pairwise"}.}

\item{point.color, point.size}{Point color and size. Defaults are
\code{"#9467bd"} and \code{0.8}. The point color also sets the boxplot color.}

\item{line.color, line.size}{Connecting line color and width. Defaults are
\code{"black"} and \code{0.5}.}

\item{theme.size}{Base font size for \code{\link[ggplot2:ggtheme]{ggplot2::theme_bw()}}. Default is \code{15}.}

\item{boxplot.width}{Boxplot width in propensity score units. Default is
\code{0.02}.}

\item{outlier.shape, outlier.size}{Boxplot outlier shape and size.
Defaults are \code{4} and \code{1}; use \code{outlier.shape = NA} to hide outliers.}
}
\value{
A \code{ggplot} object, returned without explicitly printing it, which
can be further customized with \code{ggplot2} layers and themes.
}
\description{
Plots local balance diagnostics from an M-LBCNet fit, with
points and connecting lines at generalized propensity score grid centers.
}
\details{
The primary, treatment-versus-population diagnostic has one panel per
treatment, in a single row with a shared y-axis scale. In each panel,
\code{center} refers to that treatment's generalized propensity score component.

The pairwise diagnostic has one column per treatment pair and one row per
localizing treatment, labeled \code{"Localized by <treatment>"}. For localizing
treatment \eqn{r}, both compared treatments use the same neighborhood along
\eqn{\pi_r}; the x-axis centers in that row refer to \eqn{\pi_r}.
Localizing treatments are never averaged together.

With \code{cov = "ALL"}, lines and points show the mean LSD over valid
covariates, and boxplots show the distribution of individual covariate LSD
values at selected centers. These are diagnostics for a single dataset,
not summaries over simulations or seeds. Selecting a specific covariate
shows its LSD directly, without boxplots.

Non-finite LSD values or centers, and LSD values at or above the package's
unsupported-neighborhood sentinel (\eqn{10^8}), are excluded before
summarizing. A warning reports the number of excluded observations in the
selected data. An error is raised if no valid observations remain.
}
\examples{
\dontrun{
# Assume fit is an m_lbcnet fit with treatments A, B, C and covariate Z1.
lsd_fit <- lsd(fit)

# Primary treatment-versus-population diagnostic
plot(lsd_fit)

# Specific covariate, by name or fitted column index
plot(lsd_fit, cov = "Z1")
plot(lsd_fit, cov = 1)

# Full pairwise diagnostic
plot(lsd_fit, type = "pairwise")

# Pairwise diagnostic localized by treatment A
plot(lsd_fit, type = "pairwise", localizing_treatment = "A")
plot(lsd_fit, type = "pairwise", treatment_pair = c("A", "B"))

# Disable boxplots and customize the returned plot
p <- plot(lsd_fit, box.loc = NULL)
p + ggplot2::theme(axis.text = ggplot2::element_text(size = 12))
}
}
\seealso{
\code{\link{lsd}}, \code{\link{m_lbcnet}},
\code{\link{plot.lsd}}
}
